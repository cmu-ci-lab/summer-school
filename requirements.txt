numpy==2.4.6
scipy==1.18.0
matplotlib==3.11.0
Pillow==12.2.0
pyserial==3.5
pylablib==1.4.5
pyftdi
plotly==6.8.0
ipykernel
# Camera SDKs are installed later, per vendor (see docs.html "Camera SDK"):
#   vmbpy  — Allied Vision: from the Vimba X SDK wheel, NOT from PyPI (setup.sh finds it)
#   pyueye — IDS uEye: pip install pyueye AFTER installing the IDS Software Suite
